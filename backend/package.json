{
  "name": "stripe-backend",
  "version": "1.0.0",
  "description": "Backend server for Stripe Payment Intent creation",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop stripe-backend",
    "pm2:restart": "pm2 restart stripe-backend",
    "pm2:delete": "pm2 delete stripe-backend",
    "pm2:logs": "pm2 logs stripe-backend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mime-types": "^2.1.35",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
